build/
install/
log/
__pycache__/
*.pyc
*.pyo

# colcon / ROS 2 workspace artifacts — never commit these
/build/
/install/
/log/

# colcon / ROS 2 workspace artifacts — never commit these
/build/
/install/
/log/

# colcon / ROS 2 workspace artifacts — never commit these
/build/
/install/
/log/
